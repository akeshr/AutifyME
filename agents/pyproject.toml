[project]
name = "autifyme_agents"
version = "0.1.0"
description = "The agentic service for the AutifyME Business OS."
authors = [{ name = "Abhi", email = "" }]
requires-python = ">=3.11"

[tool.uv]
# This section will be used by uv to manage dependencies, similar to a package.json
# We will add our dependencies here in the future.
